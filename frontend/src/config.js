// API配置
export const API_BASE_URL = 'http://localhost:5000'

// 其他全局配置
export const CONFIG = {
  // 支持的图片格式
  supportedImageFormats: ['.jpg', '.jpeg', '.png', '.gif'],
  
  // 默认分页大小
  defaultPageSize: 5,
  
  // 评分范围
  scoreRange: {
    min: 0,
    max: 100
  }
}
